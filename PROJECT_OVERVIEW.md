# SuperCap_2025ENTERPRIZE 项目说明（对话接力版）

> 目标：这份文档用于在后续对话中快速恢复项目上下文，尽量减少“重新读代码”的成本。内容偏工程落地，不追求论文式完整。

## 1. 项目定位

`SuperCap_2025ENTERPRIZE` 是一个基于 **STM32G474RET6** 的超级电容功率控制固件项目，核心能力是：

- 对母线与电容侧电压/电流进行高速采样
- 根据目标功率、电压状态和保护条件执行 DCDC 控制
- 在多种拓扑/工况模式间切换（Buck / Boost 及过渡）
- 通过 CAN 上报状态并接收外部指令
- 发生异常时进行硬件+软件双重保护

项目由 STM32CubeMX 生成骨架（HAL/CMSIS）+ 自定义 C/C++ 控制逻辑组成。

## 2. 硬件与工具链

## 2.1 MCU 与外设

- MCU: `STM32G474RET6`（Cortex-M4F）
- 关键外设：
  - `ADC1/ADC2`：多路模拟量采样（含 DMA）
  - `HRTIM1`：高分辨率 PWM 与快速中断控制
  - `FDCAN1`：总线通信
  - `TIM6`：低频任务调度基准
  - `I2C1/UART`：外围模块接口（如显示屏/调试）

## 2.2 构建系统

- 编译器：GNU Arm Embedded (`arm-none-eabi-gcc/g++`)
- C 标准：`gnu11`
- C++ 标准：`gnu++14`
- 优化：`-Ofast`
- 工程入口构建文件：
  - `Makefile`
  - `SuperCap.mk`

说明：项目中 C 与 C++ 混编，通过 `extern "C"` 边界衔接。

## 3. 目录与模块职责（按功能）

- `Core/Src/main.c`
  - CubeMX 初始化入口；完成时钟/外设 init 后调用 `systemStart()`。
- `Core/Src/UserTask.cpp`
  - 系统启动与周期任务组织。
  - 包含定时调度、状态机推进、通讯与接口刷新等。
- `Core/Src/PowerManager.cpp`
  - 功率级核心控制逻辑。
  - DCDC 模式判断、占空比/比较值计算、功率限制策略。
- `Core/Src/Communication.cpp`
  - CAN 收发与协议打包/解包。
  - 接收控制命令，发送状态帧（旧版/新版）。
- `Core/Src/Interface.cpp`
  - 外设接口层（含显示/上位交互相关逻辑）。
- `Core/Inc/Config.hpp`
  - 全局数据结构、参数、状态量定义（系统级“共享上下文”核心头文件）。
- `Core/Inc/PowerManager.hpp`
  - 功率管理接口与模式定义。
- `Core/Inc/Communication.hpp`
  - 通讯协议结构体、接口声明。
- `Core/Inc/Interface.hpp`
  - 界面/交互接口声明。

## 4. 启动流程（从上电到闭环）

典型链路：

1. `main()` 执行 HAL 初始化、系统时钟配置、外设初始化。
2. 调用 `systemStart()`（C++层）。
3. 在 `systemStart()` 中完成：
   - 采样与控制相关模块初始化（ADC/HRTIM/CAN/保护/接口）
   - 启动定时机制与必要中断
4. 系统进入“中断驱动 + 周期任务”运行态：
   - 高频路径：采样/控制/PWM 更新
   - 低频路径：状态机、通讯、人机接口、保护判定与恢复策略

要点：主循环不是“while里做所有事”，而是由中断与调度节拍分担。

## 5. 控制执行链路（核心）

项目可理解为三层控制链：

1. **测量层**
   - ADC + DMA 周期采样，得到输入电压、电容电压、输入/输出电流等。
2. **决策层**
   - 根据目标功率、当前电压区间、限流和故障状态，决定当前 DCDC 工作模式与控制目标。
3. **执行层**
   - 通过 HRTIM 更新比较寄存器/PWM 参数，驱动功率器件。

## 5.1 调度频率分工

- 高频控制：由 HRTIM/ADC 相关中断驱动，追求快速响应。
- 低频任务：`TIM6` 回调中运行，按 4kHz 主节拍并切分 1kHz 子任务槽执行不同任务。

这种结构常见于电力电子固件：高频闭环尽量短路径，低频做通信/状态机/慢变量管理。

## 6. DCDC 模式与状态机

已实现多模式控制（命名以代码为准）：

- `BUCK`
- `BUCKBOOST`
- `BOOSTBUCK`
- `BOOST`
- 以及若干校准/特殊模式（用于标定或特定工况）

模式切换由实时电压关系、目标输出与保护状态共同决定。`PowerManager` 负责：

- 选择模式
- 计算对应 PWM 策略
- 更新比较值/占空比
- 对突变工况进行限制（防止过冲或器件压力突增）

## 7. 保护机制（硬件优先 + 软件兜底）

保护是本项目稳定性的关键，分两层：

1. **硬件快速保护**
   - ADC watchdog / 定时器故障输入等机制可快速拉闸或禁能。
2. **软件错误管理**
   - 维护错误等级与状态机，常见分级包括：
     - `NO_ERROR`
     - 可恢复（自动恢复）
     - 可恢复（人工干预）
     - 不可恢复
     - 警告级

软件层会根据错误等级决定：

- 是否禁止输出
- 是否允许自动重试
- 是否仅告警不上闸
- 是否上报到通信状态字

## 8. 通信协议（FDCAN）

项目内存在接收与发送帧，关键 ID：

- RX：`0x061`（接收控制命令）
- TX：`0x051`（旧版状态帧）
- TX：`0x052`（新版状态帧）

`Communication.cpp/.hpp` 内定义了 old/new 两套打包结构，用于兼容不同上位机或主控协议版本。

状态上报包含典型信息：

- 电压/电流测量值
- 功率或目标相关量
- 运行模式
- 错误/告警位（bit 打包）

## 9. 条件编译：无线充电相关路径

代码中存在 `#ifdef WPT_HARDWARE` 分支，启用后会加入无线充电相关控制逻辑，例如：

- ASK 通讯路径
- E-leg（相关桥臂）控制行为

如果当前硬件版本不含该功能，需确认宏开关关闭，避免引入无效分支复杂度。

## 10. 关键数据与“看哪里最快”

阅读与调试建议优先抓这些“系统总线变量/函数”：

- 全局配置与状态：`Config.hpp` 中系统级结构体（如系统数据、采样数据、控制数据、错误数据）
- 模式切换与PWM更新：`PowerManager.cpp`
- 周期调度节拍：`UserTask.cpp`
- 上下位机协议映射：`Communication.cpp`

若要快速判断“系统为什么不出功率/为什么报错”，通常按下列顺序：

1. 先看错误等级与保护触发源
2. 再看当前模式是否切换到预期状态
3. 再看目标值是否真正写入控制环
4. 最后看 PWM 输出是否被故障链路钳制

## 11. 常见问题排查清单

1. **现象：无输出 / 输出被禁止**
   - 看保护状态是否锁死
   - 看使能条件是否满足（命令、模式、阈值）
   - 看 HRTIM 比较值是否被置零或冻结

2. **现象：模式反复跳变**
   - 看模式切换阈值是否过近
   - 看测量噪声过滤是否不足
   - 看滞回策略是否缺失或参数过小

3. **现象：CAN 有接收但状态不更新**
   - 核对 RX ID 是否为 `0x061`
   - 核对打包版本（0x051/0x052）与上位机解析一致
   - 核对发送调度是否被任务槽跳过

4. **现象：偶发复位或严重告警**
   - 重点检查硬件保护触发记录
   - 检查电流采样零偏/标定参数
   - 检查上电初期状态机是否进入危险分支

## 12. 下次对话建议提供的信息（模板）

为提高后续协作效率，下次可直接附上：

- 当前分支和最近改动文件
- 具体故障现象（“何时触发、可否复现、触发前后变量值”）
- 当前硬件版本（是否启用 `WPT_HARDWARE`）
- CAN 日志样例（至少包含 `0x061/0x051/0x052`）
- 关键实时量抓取：输入电压、电容电压、输入电流、模式号、错误码

## 13. 一句话总结

这是一个“**中断驱动的实时电力电子控制固件**”：高频回路保证控制响应，低频任务负责系统组织与通讯，`PowerManager + Protection + Communication` 共同决定系统是否安全且按目标功率运行。

---

如果后续要继续维护，建议优先沉淀两份文档：

- `协议字段对照表`（位定义、缩放比例、单位）
- `模式切换判据表`（阈值、滞回、保护联动）

这两份会显著降低新人接手成本。
